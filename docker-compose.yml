services:
  postgres:
    image: postgres:16
    container_name: creditscanai-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-creditscanai}
      POSTGRES_USER: ${POSTGRES_USER:-creditscanai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-creditscanai_dev}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-creditscanai}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
